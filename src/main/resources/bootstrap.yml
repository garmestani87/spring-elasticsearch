spring:
  application:
    name: ElasticSample
  profiles:
    active: dev
  cloud:
    config:
      enabled: true
      fail-fast: false
      uri: #your config server
      username: #user
      password: # pass
      name: #your config
